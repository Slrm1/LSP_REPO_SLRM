# AI Usage — Question 2

**AI Tools Used:** Cursor (Claude)

**Prompts Used (2–5 max):**

1. Generate Template Method report classes matching exam output format.
2. Short design paragraph referencing `generateReport` and subclass hooks.

**How AI Helped (2–3 sentences):**  
AI drafted the class skeletons and driver loop. I checked the printed layout line-by-line against the exam’s expected output and kept the base method `final` so the template stays fixed.

**Reflection (1–2 sentences):**  
This exercise made the “fixed algorithm, variable steps” idea click: the abstract class owns the process; subclasses only plug in pieces.
